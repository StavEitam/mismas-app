# Copy this file to .env and fill in real values from the Firebase console:
# Project Settings -> General -> Your apps -> Web app -> SDK setup and configuration
# These are public client identifiers (not secrets) but are still kept out of
# git so each environment (dev/staging/prod Firebase project) can differ.

EXPO_PUBLIC_FIREBASE_API_KEY=
EXPO_PUBLIC_FIREBASE_AUTH_DOMAIN=
EXPO_PUBLIC_FIREBASE_PROJECT_ID=
EXPO_PUBLIC_FIREBASE_STORAGE_BUCKET=
EXPO_PUBLIC_FIREBASE_MESSAGING_SENDER_ID=
EXPO_PUBLIC_FIREBASE_APP_ID=
